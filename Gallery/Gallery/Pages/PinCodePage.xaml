<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Gallery.Pages.PinCodePage"
             NavigationPage.HasNavigationBar="False">
  <ContentPage.Resources>
    <ResourceDictionary>
    </ResourceDictionary>
  </ContentPage.Resources>
  <ContentPage.Content>
    <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
      <Label x:Name="lblTitle" 
                Text="Вход по PIN-кодам!"
                FontSize="{DynamicResource Key=growingFont}"
                TextColor="{DynamicResource Key=growingColor}"
                Padding="30, 10, 30, 30"
                VerticalOptions="Center" 
                HorizontalOptions="Center" />
      <Entry x:Name="entPIN" 
             Placeholder="введите pin"
             PlaceholderColor="SkyBlue"
             FontSize="Medium"
             IsPassword="True"
             VerticalTextAlignment="Center"
             HorizontalTextAlignment="Center"
             Keyboard="Numeric"
             MaxLength="4"/>
      <Button x:Name="btnSave" Text="Готово" Clicked="btnSave_Clicked" TextTransform="None"/>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>